version: '3.8'

services:
  qdrant:
    image: qdrant/qdrant:v1.10.1
    container_name: qdrant-rag-prototype
    ports:
      - "6333:6333"
      - "6334:6334"
    volumes:
      - ./qdrant_storage:/qdrant/storage
    restart: unless-stopped
